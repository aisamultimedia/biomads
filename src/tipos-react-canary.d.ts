/* El App Router de Next 16 corre sobre React canary, que exporta
   `ViewTransition`. Los tipos viven en `react/canary` y hay que
   referenciarlos explícitamente. Ver src/app/template.tsx. */
/// <reference types="react/canary" />
